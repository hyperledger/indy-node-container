apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "indy.fullname" . }}-test-connection"
  labels:
    {{- include "indy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "indy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
